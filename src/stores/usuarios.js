import { defineStore } from "pinia";

export const useUsuariosStore = defineStore("usuarios", {
  state: () => {
    return {
        usuarios: [],
    };
  },

  getters: {
    
  },

  actions: {
    
  },
  persist: true,
});
